import type { TokenRuleMap } from "./token-rules";

// Overlay describing per-token operation availability.
// Populate with exceptions; leave empty for default behaviour.
export const TOKEN_RULE_OVERLAY: TokenRuleMap = {};
