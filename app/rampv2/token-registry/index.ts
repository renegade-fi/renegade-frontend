/**
 * Token registry for managing supported tokens and their capabilities.
 */
export {
    canUnwrapToEth,
    getAllTokens,
    getSwapInputsFor,
    getTokenByAddress,
    getTokenByTicker,
    type Token,
} from "./registry";
